import {requireEditor} from "@/lib/auth/roles";import {addClaim} from "../actions";
export default async function Page(){const {supabase}=await requireEditor();const {data:claims}=await supabase.from("claims").select("*").order("created_at",{ascending:false});return <div className="mx-auto grid max-w-[1400px] gap-12 px-6 py-12 md:grid-cols-[430px_1fr]"><section><h1 className="font-serif text-4xl">Claim toevoegen</h1><form action={addClaim} className="mt-7 space-y-3"><input className="meridian-field" name="title" required placeholder="Korte titel"/><textarea className="meridian-field" name="statement" rows={6} required placeholder="Exacte claim"/><select className="meridian-field" name="claim_type">{['factual','causal','interpretive','experiential','hypothesis','normative'].map(v=><option key={v}>{v}</option>)}</select><select className="meridian-field" name="evidence_status">{['unverified','reviewing','supported','mixed','disputed','rejected','unknown'].map(v=><option key={v}>{v}</option>)}</select><input className="meridian-field" name="confidence" type="number" min="0" max="1" step="0.05" defaultValue="0"/><input className="meridian-field" name="scope" placeholder="Scope"/><button className="bg-[#102633] px-5 py-3 text-sm text-white">Claim opslaan</button></form></section><section><h2 className="font-serif text-3xl">Claimregister</h2><div className="mt-5 border-t">{claims?.map(c=><article key={c.id} className="border-b py-5"><p className="text-xs uppercase tracking-[0.1em] text-[#ad6540]">{c.claim_type} · {c.evidence_status}</p><h3 className="mt-2 font-serif text-xl">{c.title}</h3><p className="mt-2 text-sm leading-6 opacity-60">{c.statement}</p></article>)}</div></section></div>}
